# Config

This folder holds environment settings used by the Azure migration project.

Files:
- `local.yml`
- `dev.yml`
- `test.yml`
- `prod.yml`

Keep secrets out of these files. Use placeholders or reference a secret store instead.
